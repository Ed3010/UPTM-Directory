<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FloorPlans" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FloorPlans" parent="Base.Theme.FloorPlans" />

    <style name="CustomTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textSize">8sp</item>
        <item name="android:textColor">#000000</item> <!-- Black color -->
        <!-- You need to replace "poppins_regular.ttf" with the actual font file name -->
        <item name="android:fontFamily">@font/poppins</item>
    </style>
</resources>